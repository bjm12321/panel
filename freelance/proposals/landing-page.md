# Proposal template — landing page / small static site / site fix

Use for: one-page sites, marketing pages, HTML/CSS fixes, small static builds.
Fill from the listing; leave `[[OPERATOR: ...]]` slots for the operator.
Keep the final proposal under ~150 words.

---

Hi [[OPERATOR: client first name if shown]],

You want {one-sentence restatement: what the page is for and who it has to
convince, in their words}.

What I'd deliver:

- A {single-page site / fixed page} built as clean, self-contained HTML/CSS —
  no frameworks to maintain, loads fast, looks right on phones
- A live preview link before you accept anything, so revisions happen on a
  real page, not a mockup
- The final files ready to drop into {their host if named, else "any static
  host — Netlify, GitHub Pages, your existing server"}

I work with modern AI-assisted tooling, which is how I can show you a first
version in {24–48h}, and I test on desktop + mobile myself before handoff.
{If a design/brand exists: "I'll match your existing branding exactly — send
whatever you have."}

One question: {sharpest scope question — usually about copy: do they provide
text/images, or is drafting included?}

[[OPERATOR: one personal line referencing their business or the listing.]]

— [[OPERATOR: name]]

---

**Acceptance checklist to include in delivery** (edit per job): renders
correctly on mobile + desktop; all links/forms work; images optimized; copy
approved by client; passes a Lighthouse check without red flags.
