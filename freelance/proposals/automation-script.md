# Proposal template — automation / data script

Use for: Python scripts, CSV/Excel cleanup, format conversion, API glue,
client-authorized scraping. Fill from the listing; leave `[[OPERATOR: ...]]`
slots for the operator. Keep the final proposal under ~150 words — buyers skim.

---

Hi [[OPERATOR: client first name if shown]],

You need {one-sentence restatement of their problem, in their vocabulary —
prove the listing was read}.

Here's exactly what I'd deliver:

- {the script/tool, named concretely: "a Python script that takes X and
  outputs Y"}
- A sample run against {their data if provided, otherwise "test data matching
  your format"} so you can see the output before you accept
- A short README so anyone on your side can run it

I build with modern AI-assisted tooling, which is why I can turn this around
in {24–48h for small, else realistic estimate} — and I personally test every
deliverable before handoff.

One question before I start: {single sharpest scope question from the
listing's biggest ambiguity}.

[[OPERATOR: one personal line — why this job caught your eye, or a relevant
detail from the client's profile. Delete the proposal rather than skip this.]]

— [[OPERATOR: name]]

---

**Acceptance checklist to include in delivery** (edit per job): runs on their
sample data without errors; output matches the agreed format; README covers
setup + one example command; edge cases named in the listing are handled.
